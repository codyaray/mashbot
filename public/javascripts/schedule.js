function init_schedule(){
  $('#schedule').fullCalendar({});
  $('#accordion').accordion();
}

function CalEvent(config) {
  this.id = undefined; // Integer or String - Unique unless repeating event
  this.title = undefined; // String
  this.allDay = true; // Boolean
  this.date = undefined; //
  this.start = undefined;
  this.
  this.init = function(config) {

  };

  this.init(config);
}